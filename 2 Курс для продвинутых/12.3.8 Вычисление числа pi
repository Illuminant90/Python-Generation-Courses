from random import uniform

n = 10**6       # количество испытаний
k = 0
s_sqr = 2 * 2
for _ in range(n):
    x = uniform(-1, 1)
    y = uniform(-1, 1)
    if x ** 2 + y ** 2 <= 1:
        k += 1
s_cyrc = (k / n ) * s_sqr
r = 1
pi = s_cyrc / r ** 2
print(pi)