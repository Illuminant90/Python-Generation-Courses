from sys import stdin
from collections import Counter

studenst_grades = Counter()
for line in stdin:
    student, grade = line.split()
    studenst_grades.update({student: int(grade)})
print(studenst_grades.most_common()[-2][0])