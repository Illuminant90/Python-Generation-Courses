from datetime import datetime

with open('diary.txt', encoding='utf-8') as fl:
    diary = dict()
    new_record = False
    pattern = '%d.%m.%Y; %H:%M'
    for line in fl:
        try:
            time_record = datetime.strptime(line, pattern)
            print(time_record)
            continue
        except:
            pass
